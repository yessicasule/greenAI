# Credibility & Accuracy Verification Report

**Project:** Green-Weight — Complexity-Aware Dynamic Precision Routing for
Energy-Proportional LLM Inference
**Report date:** 2026-07-10 · **Status: PRE-MEASUREMENT** (see §1)

This document is the project's single source of truth on which claims are
verified, how they were verified, and which claims must NOT yet be made.
It is updated after every measurement session; the machine-checked half of
it is generated by `training/scripts/verify_results.py` into
`backend/src/green_weight/results/results_validation.md`.

---

## 1. Current verification status — honest summary

| Claim | Status | Evidence |
|---|---|---|
| "Dynamic precision routing saves ~40% energy" | **UNVERIFIED — do not state** | Number derives from an assumed linear bit-width energy model, not measurement. No measured data exists as of this date. |
| "Accuracy loss < 1%" | **UNVERIFIED — do not state** | Accuracy pipeline produced `{}` in all May 2026 runs (lm-eval missing). |
| Per-tier J/token | **VERIFIED 2026-09-04** | SPIT cluster (RTX 6000 Ada, driver 610.43.02): 4bit 1.414±0.055, 8bit 3.052±0.039, 16bit 8.124±0.241 J/token (n=1500/tier, 3 runs, seed=42). 117 rows (2.6%) have energy_j=0 from 1-token prompts — real measurements, not meter failure. Tiers separate monotonically (~2.16× per step). See paper/results.md. |
| Per-tier benchmark accuracy (±QAT adapters) | **PENDING** | Kaggle Session 2 (`kaggle_accuracy_eval.py`). |
| Routing accuracy/energy across 8 conditions | **PENDING** | Kaggle Session 4 (`kaggle_routing_experiment.py`). |
| Fuzzy controller & complexity sensor implemented as described | **VERIFIED** | Code inspection, 2026-08-05: `backend/src/green_weight/router/fuzzy_controller.py` (real Mamdani fuzzy-inference system via `scikit-fuzzy`), `backend/src/green_weight/router/complexity_scorer.py` (5 features: Flesch-Kincaid, token length, entropy, syntax depth, has_code_or_math). This is the canonical implementation — `training/scripts/kaggle_routing_experiment.py` imports these exact modules rather than embedding a copy. An earlier, divergent implementation (`core/`+`controllers/`) existed and was what the Session 4 script previously embedded; it's archived at `backend/src/green_weight/_legacy/` (see its README) and is not part of the live system. |
| RouteLLM bridge integration | **NOT REAL — documented pass-through by design** | `backend/src/green_weight/router/routellm_bridge.py` applies fixed thresholds to the fuzzy controller's `win_probability`; it does not load or call a RouteLLM model. RouteLLM's pretrained checkpoints are trained on human-preference battles between specific named models (GPT-4, Mixtral, ...) and have no learned signal for our quantization tiers — using one as-is would produce a plausible-looking but meaningless number. A real tier-preference router, trained on Session 4's `routing_per_prompt.csv`, is planned as a post-Session-4 addition (RQ3) — until that exists, no paper claim of "RouteLLM integration" is valid. |
| Fuzzy controller's feature-normalization bounds are empirically grounded, not arbitrary | **RE-CALIBRATED 2026-08-13, FULLY RESOLVED 2026-08-22** | `ENTROPY_RANGE` and `SYNTAX_DEPTH_RANGE` in `complexity_scorer.py` re-validated against the real 500-prompt `eval_prompts.jsonl` (superseding the 2026-08-05 30-prompt calibration): old bounds were clipping real data at the top (entropy max 4.66 > old bound 4.5; syntax_depth max 12 > old bound 8, hitting exactly the hardest ~5% of prompts). Fixed: `ENTROPY_RANGE` (3.0,4.5)→(3.3,5.0), `SYNTAX_DEPTH_RANGE` (1,8)→(2,14). Per-difficulty analysis shows `entropy` barely discriminates prompt difficulty (easy mean 4.11 vs hard mean 4.11 bits — nearly identical); `syntax_depth` does discriminate (easy 4.42/medium 4.46/hard 5.57). Range-widening doesn't fix entropy's weak signal — flagged as a candidate finding for the Phase 5c feature-ablation study. **2026-08-22 (off-campus, no GPU access):** `config.yaml` breakpoints updated to match — `entropy_breakpoints` [3.3,3.7,4.1]→[3.8,4.1,4.5] (this was a real bug, not cosmetic: old `bps[0]`=3.3 exactly equalled the new `ENTROPY_RANGE` floor, giving `normalize_to_01(3.3,3.3,5.0)=0` and a zero-width degenerate "low" membership triangle), `syntax_depth_breakpoints` [3,4,6]→[4,5,8] (old values compressed into the bottom ~17% of [0,1] against the widened range, pushing nearly all real prompts into "high" regardless of actual difficulty). Also fixed the previously-flagged `token_length` issue: added `TOKEN_LENGTH_RANGE=(0,154)` to `complexity_scorer.py`, replacing the hardcoded 512-token cap — computed directly from `eval_prompts.jsonl` (min=5.25, max=153.75, p95=62.0, p99=109.25 approx-tokens), confirming the "~154/p95=62" estimate exactly. Re-ran the fuzzy controller on 5 known prompts post-fix: sane tier assignments (trivial→4bit, code-gen→16bit), no crashes. **Resolved same session:** the `_trimf_low_mid_high()` dead-3rd-breakpoint issue found above. Dropped the dead value from all four breakpoint lists in `config.yaml` and documented that only 2 edges (`lo`/`hi`) are used; MEDIUM's peak is their auto-computed midpoint. Zero behavior change (re-verified against the same 5 sanity-check prompts — identical `win_probability` outputs). True 3-breakpoint asymmetric partitioning was considered and deferred to Phase 5c: it would shift routing behavior for every feature and additionally require re-tuning `flesch_kincaid_breakpoints` (its old 3rd value, 18, sat exactly on `FLESCH_KINCAID_RANGE`'s ceiling — the same zero-width degenerate-membership bug class just fixed for entropy). See `NEW.md` Phase 4 for full detail. |
| Three QAT LoRA adapters exist, base = Llama-3.2-1B, and are loadable | **FULLY VERIFIED 2026-08-22** | Static check (`PeftConfig`/safetensors parsing, tier→rank mapping) done first, then a **real load test**: user provided `HF_TOKEN` (Windows user-scope env var, never pasted into chat), downloaded the actual gated `meta-llama/Llama-3.2-1B` weights on CPU (~141s), ran `PeftModel.from_pretrained(base_model, adapter_path)` for real for all three adapters, each followed by one short greedy generation. All three attached and generated without error; base model included as a control, identical output across all four (expected for a trivial factual completion under greedy decoding — not a red flag). Phase 3's gate is now genuinely met, not just statically approximated. Their *effect* on accuracy vs. plain PTQ is still PENDING (Session 2). |
| Live demo/API accuracy path (`benchmark/accuracy_eval.py`) produces real numbers, not fabricated ones | **FIXED 2026-08-22** | Was fabricating data: `_evaluate_task_fixed_tier()`/`_evaluate_task_routed()` never called `lm_eval`, returning hardcoded dicts (e.g. `{"mmlu": {"4bit": 0.42, ...}}`) regardless of input; `compute_routellm_metrics()` hardcoded `cpt=0.5`. This is the live demo/API path (`run_pipeline.py` Phase 5) — confirmed separate from the real Session 2 script (`training/scripts/kaggle_accuracy_eval.py`, verified to genuinely call `lm_eval.simple_evaluate()`), so no paper number was ever affected — but running `run_pipeline.py` outside dry-run would have silently written fabricated numbers to `accuracy_results.json`. Rewritten to wrap loaded tier models in lm-eval's `HFLM` and call `lm_eval.simple_evaluate()` for real; added a `RoutedLM` class that dispatches each individual lm-eval request to the fuzzy controller's tier choice for that request's prompt text (genuine per-example routing, not an aggregate approximation). Every failure path now raises `RuntimeError` instead of defaulting to a fake score. Verified without GPU: module imports cleanly, evaluator correctly raises when no model is loaded, `RoutedLM`'s dispatch/grouping/reassembly unit-tested against mock tier-LMs (order-preserving across mixed-tier batches). **Not yet verified: an actual run against real loaded models** — needs the cluster. |
| Live pipeline's QAT adapters actually get applied | **BUG FOUND AND FIXED 2026-08-22** | `models/model_pool.py`'s `load_pool()` looked for adapters at `backend/src/green_weight/adapters/adapter_{4bit,8bit,16bit}` — a path that has never existed (real adapters live at `major-project/adapters/adapter_{simple,medium,complex}`). Every existence check silently returned `False`, so the live pipeline has been running plain post-training quantization with no QAT adapter applied, with zero errors or warnings, since this code was written. Fixed the path (`Path(__file__).resolve().parents[4]`) and the three directory names; verified the corrected path resolves to the real, existing adapter directories on disk. |
| `api.py`'s `/infer` endpoint can actually run real GPU inference | **BUG FOUND AND FIXED 2026-08-22** | `_model_pool_loaded` was declared `False` at module level and never set to `True` anywhere (confirmed via repo-wide grep) — `startup()` only initialized the fuzzy controller/bridge, never called `model_pool.load_pool()`. `/infer`'s real-inference branch was permanently dead code (every request silently used the mock responder, correctly labeled `is_mock: True`) and `/health`'s `gpu_ready` was permanently `False`. Fixed by wiring `load_pool()` into `startup()`, gated behind `GREEN_WEIGHT_LOAD_MODELS=1` (unconditional loading would break `uvicorn --reload` dev ergonomics — it restarts `startup()` on every file save). Verified with FastAPI's `TestClient`: default behavior unchanged, and the opt-in path degrades gracefully without CUDA (no crash, clear warning). The real-GPU-and-opted-in path still needs a cluster to verify end-to-end. |
| `training/scripts/kaggle_energy_benchmark.py` and `kaggle_routing_experiment.py`: `GpuEnergyMeter.info()` crash risk | **BUG FOUND AND FIXED 2026-08-22** | Neither decoded `pynvml.nvmlDeviceGetName()`/`nvmlSystemGetDriverVersion()`, though `check_nvml_energy_support.py` explicitly anticipates these returning `bytes` on some pynvml versions. Would have crashed `json.dumps()` — in Session 1's case, before any measurement runs at all. Fixed both with the same defensive `.decode()` pattern. Compile-checked only; not runnable end-to-end without a GPU. |
| `kaggle_routing_experiment.py`'s condition 6 (`threshold_router`) is a real non-fuzzy baseline, not a tautology | **BUG FOUND AND FIXED 2026-08-22, before any Session 4 GPU time spent** | Was feeding `win_probability*100` (the fuzzy controller's own fully-defuzzified output) through the identical 33/66 cut points `FuzzyController` already applies internally — `threshold_router`'s tier was mathematically guaranteed to equal the raw `fuzzy_tier` for every prompt, defeating RQ3's whole point ("does the fuzzy inference system beat a naive threshold"). Confirmed empirically: 5/5 test prompts matched exactly under the old code. The function's own prior docstring said "instead of fuzzy membership," directly contradicting the implementation. **User chose the fix** (of three options presented: mean-of-raw-features / single-dominant-feature / reframe-the-docs-only): added `naive_complexity_score()` — a plain mean of the 5 raw complexity features, computed without calling `FuzzyController` — and rewired `threshold_router` to use it. Verified the fix breaks the tautology: only 2/5 test prompts now match the fuzzy tier (genuine divergence). Also added a `naive_complexity` column to `routing_per_prompt.csv` for auditability. This is the most significant finding of the 2026-08-22 implementation-hardening pass — had it shipped as-is, Session 4's RQ3 comparison would have been scientifically invalid. |
| Documented QAT retraining script is the one that actually produced the real adapters | **BUG FOUND AND FIXED 2026-08-22, before any retraining was ever attempted** | Every doc (`RESEARCH_PLAN.md`, `KAGGLE_MANUAL.md`, `training/configs/README.md`, and this project's own `CLAUDE.md`) pointed at `training/scripts/kaggle_qat_trainer.py` as the QAT training entrypoint. Diffed its `LoraConfig` against the real adapters' `adapter_config.json`: they don't match at all — this script uses r=32/16/8 with 7 `target_modules` (`q_proj,k_proj,v_proj,o_proj,gate_proj,up_proj,down_proj`) and `adapter_4bit/8bit/16bit` naming, while the real adapters use r=16/8/4, `target_modules=[q_proj, v_proj]`, and `adapter_simple/medium/complex` naming. Found the actual matching script: `training/scripts/adapter-training.ipynb` — its inline `train_adapter("adapter_simple", r=16, alpha=32)` / `r=8,alpha=16` / `r=4,alpha=8` calls match the real adapters exactly. `kaggle_qat_trainer.py` and its notebook counterpart `major-project-v2.ipynb` are a stale, unused pair that were never archived to `_legacy/` the way the old `core/`/`controllers/` implementation was, despite being just as superseded. Had this gone unnoticed and a retrain been triggered (Phase 3's original decision tree: "retrain if they fail to load or underperform"), the wrong script would have produced adapters inconsistent with the ones already verified working. Fixed every doc reference; added a prominent stale-script warning header to `kaggle_qat_trainer.py` and a warning cell to `major-project-v2.ipynb`. Two further orphaned notebooks (`majorproject-final.ipynb`, `majorproject-final (1).ipynb`) identified as earlier, broader exploratory pipelines (install `routellm`/`codecarbon` directly, predate the `router/` refactor) — not referenced as canonical anywhere, flagged but not yet formally archived. |
| Kaggle Session 1's original failure mode is understood | **RESOLVED 2026-08-22, together with the user (their Kaggle account)** | Found the notebook (`new_and_improved_greenai`, kernel 130610077 — not one of the 4 `.ipynb` files in `training/scripts/`, so not locally auditable) and read its version history/logs directly. Two distinct failures found: Version 4 was a quick, clean `BackendError` (Kaggle secret not attached under the expected label — unrelated, easily avoided). **Version 3 is the real incident**: normal operation to ~7.3h in (during 8-bit-tier work), then a burst of ~700+ repeated `MatMul8bitLt` bitsandbytes casting warnings within a 0.4s window, then complete silence until Kaggle's 12h session cap force-killed it (exit code 137) — a genuine silent hang, not a clean OOM/quota error. This is exactly the class of risk that could recur on the college cluster, not something the platform switch automatically fixes. Actionable takeaway for Phase 1: monitor early progress on the cluster rather than starting a session and walking away for hours. Could not confirm whether this specific hang risk still exists in the current, already-hardened `kaggle_energy_benchmark.py` — the old notebook's code isn't in the repo to diff against. |
| `frontend/` doesn't display fabricated/mislabeled/corrupted content | **BUGS FOUND AND FIXED 2026-08-22** | User-requested deep frontend audit (previously only checked for dead files). Found: (1) `Playground.jsx` had real double-UTF-8 mojibake baked into the file bytes, 7 distinct corrupted sequences / 14 occurrences (confirmed via raw byte inspection) — fixed with targeted replacement, deliberately not a blanket encoding round-trip (would have corrupted legitimately-encoded `·` separators elsewhere in the same file); (2) the "Llama-2 vs Llama-3.2-1B" bug from the 2026-07-10 backend audit was independently reintroduced twice in the frontend — `Playground.jsx` and `Settings.jsx` both hardcoded the old `meta-llama/Llama-2-7b-hf` in their config-preview displays; (3) `api.py`'s `_append_trace()` didn't log `is_mock`, so `pipeline_trace.jsonl` couldn't distinguish real from mock energy after the fact; (4) `Analytics.jsx`'s "Energy Saved" KPI showed a fabricated "100% saved" result whenever nothing had actually been measured (0 real energy / 0 baseline = 100%) — exactly the class of bug this session has been hunting; (5) `Analytics.jsx`'s trade-off scatter chart labeled itself "live" while its energy axis was always the same hardcoded demo constants (`8.5/28.5/36.8/105`J) regardless of real accuracy data — matches an already-documented known risk in `verification/checklist.md` about those exact placeholder values. All fixed and verified for real: `npm run build` succeeds cleanly, and the actual Vite dev server was driven with the Browser tool (worked around a test-environment-only issue where the tool's non-composited tab pauses Framer Motion's page-transition animation — not a real app bug, confirmed by bypassing the transition and rendering each page as the initial mount instead). |
| `backend/tests/` has a real, passing test suite | **BUILT 2026-08-22** | Was completely empty (only `.gitkeep`) — no test suite existed anywhere in this project. Built via a `test-agent`-scoped subagent in an isolated worktree; since this repo isn't a git repository, that isolation fell back to a plain directory snapshot taken at an earlier, pre-fix point, so the subagent's own view of the codebase was stale (correctly detected via byte-for-byte diff, application code left untouched per scope, tests written against what was actually live there). Reconciled by hand against the real fixed codebase: 5 files merged as-is (already compatible), `test_complexity_scorer.py` gained `TOKEN_LENGTH_RANGE` coverage, `test_config.py` had 4 stale breakpoint-list assertions corrected, `test_known_issues.py` deleted (all 3 bugs it xfail-documented are now fixed elsewhere), `test_accuracy_eval.py` fully rewritten against the real `RoutedLM`/`_extract_scores`/`RuntimeError` behavior. **Verified: `python -m pytest backend/tests -q` → 146 passed, 0 failed**, reproducible from a different cwd, no stray output in the real repo tree. |

**Bottom line: as of this report, the project makes no verified quantitative
claim.** The measurement infrastructure below exists precisely so that every
number in the eventual paper is traceable to a raw CSV, a script, and a
recorded GPU.

## 2. Defects found and fixed (2026-07-10 audit)

These previously invalidated any result the old pipeline could have produced:

| Defect | Fix |
|---|---|
| Energy computed by inverting CO₂ emissions with a wrong constant (`energy_tracker.py`) | Now reads CodeCarbon's `energy_consumed` (kWh) directly; Kaggle scripts bypass CodeCarbon entirely for the NVML hardware counter. |
| J/token divided by a hard-coded "50 tokens" | Real generated-token counts recorded per inference. |
| `energy_summary.csv` empty across all runs (zero measurements) | Validator now hard-FAILs on empty logs; scripts flush incrementally. |
| Config model (`Llama-2-7b`) ≠ adapter base (`Llama-3.2-1B`) | Unified on `meta-llama/Llama-3.2-1B`. |
| Eval set: 50 rows, only 30 unique (duplicates from repetition-padding) | Deduplicated; `prepare_eval_dataset.py` rewritten to build 500 unique stratified prompts from TriviaQA/Alpaca/GSM8K/CodeAlpaca and to fail loudly rather than pad. |
| HF token placeholder committed in CLAUDE.md | Removed; secrets policy documented in KAGGLE_MANUAL.md. |
| Simulated inference responses could reach benchmark paths | Simulation confined to demo; measurement scripts always run real models and record hardware provenance. |

## 3. How energy numbers are verified

**Instrument.** NVML `nvmlDeviceGetTotalEnergyConsumption` — the GPU's
on-board cumulative energy counter (mJ resolution, supported on the T4).
This is a hardware measurement, not a software estimate. If a node lacks
the counter, the script falls back to integrating 20 Hz power samples and
the validator flags the run with a WARN that must be disclosed.

**Protocol.** Greedy decoding (deterministic), fixed `max_new_tokens=128`,
5 warmup generations before measuring, one tier in GPU memory at a time,
`torch.cuda.synchronize()` around timings, actual generated-token counts,
idle-power baseline and GPU/driver identity recorded in
`hardware_info.json`. Full sweep repeated 3× (different days/nodes);
reported as mean ± 95% CI.

**Derivation rule (Session 4).** Because greedy decoding is deterministic,
a router's per-prompt response and energy equal those of the tier it picks.
Phase A measures every prompt on every tier; every routing condition is then
an exact function of measured data — nothing is modeled. Router compute
overhead is measured separately and reported (it runs on CPU in
milliseconds; claiming it as zero would be wrong, so it is quantified).

**Automated gate.** `training/scripts/verify_results.py` checks every results file
for: non-emptiness, minimum n per tier (≥30), positive energies and token
counts, J/token inside a physical plausibility window for a 1B model on a
T4 (0.05–50 J/token), CI tightness, ≥3 repeated runs, complete
prompt×tier measurement grids, and internal consistency (oracle must be the
accuracy maximum; a 4-bit-costs-more-than-fp16 result triggers the §5 risk
warning). **Policy: a number appears in the paper only if its checks PASS;
WARNs must be acknowledged in the paper's threats-to-validity section.**

## 4. How accuracy numbers are verified

- **Benchmark accuracy** comes from lm-eval-harness (community-standard,
  versioned tasks), seeds fixed at 42, tiny-Benchmarks tasks or standard
  tasks with a documented example limit. Raw JSON output is kept.
- **Routed-set correctness** uses a reference-match proxy (numeric match for
  math, normalized substring for short facts, token-F1 ≥ 0.5 otherwise).
  This is a *proxy* and is labeled as such everywhere; the paper's
  benchmark-accuracy claims come only from lm-eval.
- Sanity cross-checks: accuracy ∈ [0,1]; fp16 not beaten by 4-bit beyond
  noise (else the eval setup is investigated before anything is reported).

## 5. Known threats to validity (to be reproduced in the paper)

1. **Hardware scope.** All measurements are on one GPU class (T4). Findings
   may differ on A100/H100 or CPUs. Claims will be scoped to "commodity
   inference GPUs of the T4 class."
2. **Quantization kernel reality.** bitsandbytes dequantizes to fp16 for
   compute; low-bit tiers may not save energy proportionally to bit-width —
   this is measured, not assumed, and the outcome is reported either way.
3. **Model scope.** One model (Llama-3.2-1B). No claim is made about other
   sizes/families without measurement.
4. **Correctness proxy.** Reference-match correctness on the routed set is
   an approximation (§4); oracle conditions inherit its noise.
5. **Shared-infrastructure noise.** Kaggle nodes vary in thermals/hosts;
   mitigated by 3 repeat runs on different days and CI reporting, and
   disclosed regardless.
6. **Energy counter scope.** NVML measures GPU-board energy only; CPU/DRAM
   host energy is excluded and stated as such.

## 6. Reproducibility artifact (released with the paper)

- All scripts in `training/scripts/` (measurement, evaluation, routing, figures,
  validation) — pinned seeds, documented protocol.
- Raw per-inference CSVs (`energy_per_inference.csv`,
  `routing_per_prompt.csv`) and `hardware_info.json` per run.
- The generated `results_validation.md` for each run, so reviewers can see
  the same gate we applied to ourselves.
- The eval-set builder (`prepare_eval_dataset.py`) with its seed, so the
  exact 500-prompt set is reconstructible.

## 7. Sign-off checklist before submission

- [ ] Session 1 done ×3 runs; validator PASS; Table 1 filled from
      `energy_summary.csv`.
- [ ] Session 2 done; validator PASS; RQ4 answered from
      `accuracy_summary.csv`.
- [ ] Session 4 done ×3 runs; validator PASS; Pareto figure regenerated
      from real CSVs.
- [ ] Every WARN from `verify_results.py` either resolved or written into
      the threats-to-validity section.
- [ ] No sentence in the paper contains a number that cannot be traced to a
      committed CSV.
- [ ] §1 of this report updated: no claim left in UNVERIFIED/PENDING state
      is present in the paper.
